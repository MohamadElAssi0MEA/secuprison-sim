Frontend: React + D3 simulation.

Local dev:
> npm install
> npm run start
Open http://localhost:5173

Build for production:
> npm run build
The build outputs go to /dist and are used for GitHub Pages deployment.

Learner Mode: guided simulation, stores results in localStorage, exportable as CSV.
Operator Mode: emits logs for technical evaluation (latency, mapping accuracy).
